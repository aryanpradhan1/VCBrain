# Superseded -- the whole backend now uses one canonical env file at backend/.env
# (see backend/.env.example), loaded automatically by backend/api/main.py on startup.
# This module-local file still works as a fallback if you genuinely want to run
# diligence_memo in isolation (see clients.py's _dotenv_value), but for running the app
# normally, set up backend/.env instead -- not this file.
