# Superseded -- install from the repo's one complete list instead:
#   pip install -r ../requirements.txt
# (from backend/scoring/) or -r backend/requirements.txt from the repo root. This file is
# a strict subset of it.
